server:
  port: ${SERVER_PORT:8010}
spring:
  application:
    name: organization
  cloud:
    nacos:
      discovery:
        server-addr: ${REGISTER_HOST:182.254.208.28}:${REGISTER_PORT:8848}
      config:
        server-addr: ${REGISTER_HOST:182.254.208.28}:${REGISTER_PORT:8848}
        file-extension: yml
